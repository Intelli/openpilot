diff --git b/.gitattributes a/.gitattributes
index ef7a77288f..d7e945ddd7 100644
--- b/.gitattributes
+++ a/.gitattributes
@@ -10,6 +10,10 @@
 *.otf filter=lfs diff=lfs merge=lfs -text
 *.wav filter=lfs diff=lfs merge=lfs -text
 
+# keep custom tizi engage/disengage sounds as regular git blobs
+selfdrive/assets/sounds/engage_tizi_custom.wav -filter -diff -merge -text
+selfdrive/assets/sounds/disengage_tizi_custom.wav -filter -diff -merge -text
+
 # exclude auto-lock assets from LFS
 auto-lock/lock-closed-white.* -filter -diff -merge -text
 
diff --git b/selfdrive/ui/soundd.py a/selfdrive/ui/soundd.py
index cea22655b1..a8ec42db6b 100644
--- b/selfdrive/ui/soundd.py
+++ a/selfdrive/ui/soundd.py
@@ -58,8 +58,8 @@ sound_list: dict[int, tuple[str, int | None, float]] = {
 }
 if HARDWARE.get_device_type() == "tizi":
   sound_list.update({
-    AudibleAlert.engage: ("engage_tizi.wav", 1, MAX_VOLUME),
-    AudibleAlert.disengage: ("disengage_tizi.wav", 1, MAX_VOLUME),
+    AudibleAlert.engage: ("engage_tizi_custom.wav", 1, MAX_VOLUME),
+    AudibleAlert.disengage: ("disengage_tizi_custom.wav", 1, MAX_VOLUME),
   })
 
 def check_selfdrive_timeout_alert(sm):
